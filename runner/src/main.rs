//use crate::lexer::headers;

fn main() {
    //headers::parse(String::from("# Hello World"));
    println!("Hello, world!");
    // for each line in a file, parse the line
    // and write the result to a new file
    // let mut args = std::env::args();
    // args.next();
    // let filename = "./tests/markdown.md";
    // let contents = std::fs::read_to_string(filename).expect("Something went wrong reading the file");
    // let mut output = String::new();
    // for line in contents.lines() {
    //     let result = headers::headers::parse_markdown_line(line.to_string(), );
    //     output.push_str(&result);
    //     output.push_str("\n");
    // }
    // println!("{}", output);
}
